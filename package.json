{
  "name": "productivity-dashboard",
  "version": "1.0.0",
  "description": "A productivity dashboard with timer, tasks, and quick links",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0",
    "fast-check": "^3.15.0"
  }
}
